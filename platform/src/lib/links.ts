/* Outward-facing links.
 *
 * SLACK_INVITE_URL is intentionally empty. Operations/workspace.md still lists
 * the booklesss20 invite link as TODO ("generate from booklesss20 … and paste
 * here before any marketing goes out"), the old bookless10 link is dead, and
 * BOO-7 parked what Slack's role even is now the paid product is moving off it.
 *
 * So the community row renders inert until this is filled in — a wrong invite
 * link in the product is worse than no link. Paste the real one here and the
 * row goes live; nothing else needs changing. */
export const SLACK_INVITE_URL = "";
