# Booklesss Bucket

General drop zone for raw assets (logos, images, exports) before they're organized into the project.
Mirrors the Partnr Bucket pattern.

Build-consumed brand assets live in `_dev/brand/`; fonts in `_dev/fonts/`.
