# Feedback log

Every reaction the owner has had to a generated reader step, newest first.
Append-only — entries are never edited or pruned, because "we've been here
before" is the strongest signal that something should become a rule.

Format:

```markdown
### YYYY-MM-DD · course/lesson/step
- `tag` — what they said, in their words. What changes as a result.
→ promoted: **X-n**   (or → one-off)
```

Tags: `writing` · `element` · `structure` · `content`

---

*(no entries yet — first review pending)*
